<!--app/views/static_pages/show.html.erb-->
<% if @page %>
  <div class="static-page">
    <h1><%= @page.title %></h1>
    <div class="page-content">
      <%= @page.content.html_safe %>
    </div>
  </div>
<% else %>
  <p>The page content is temporarily unavailable.</p>
<% end %>
